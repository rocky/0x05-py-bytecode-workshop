* Modify understanding/modifying/example2.pyc so that also skips the "password" check. It should prompt for a value though
* Modify understanding/modifying/example3.pyc so that it reverse the check. That is, when the password is good, treat it as bad and when it is bad treat it as good.
